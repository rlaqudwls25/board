<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.board.mapper.BoardMapper">

    <select id="boardCount" resultType="integer">
        SELECT
        COUNT(*)
        FROM BOARD
    </select>

    <select id="boardList" resultType="com.board.domain.BoardDTO">
        SELECT
        bno, subject, content, writer, regdate
        FROM BOARD
        ORDER BY bno DESC
    </select>

    <select id="boardListService" resultType="hashmap" parameterType="hashmap">
        <![CDATA[
SELECT
*
FROM
board
WHERE
DEL_CHK ='N'
ORDER BY BNO DESC
LIMIT #{pageStart}, #{perPageNum}
]]>
    </select>

    <select id="countBoardList" resultType="Integer">
        <![CDATA[
            SELECT
            count(*)
            FROM
            board
            WHERE
            DEL_CHK ='N'
        ]]>
    </select>

    <select id="boardDetail" parameterType="int" resultType="com.board.domain.BoardDTO">
        SELECT
        bno, subject, content, writer, regdate
        FROM BOARD
        WHERE
        BNO = #{bno}
    </select>

    <insert id="boardInsert" parameterType="com.board.domain.BoardDTO">
        INSERT INTO
        BOARD (BNO, SUBJECT, CONTENT, WRITER, REGDATE)
        VALUES (#{bno}, #{subject}, #{content}, #{writer}, now())
    </insert>

    <update id="boardUpdate" parameterType="com.board.domain.BoardDTO">
        UPDATE BOARD
        <set>
            <if test="subject != null and subject != ''">SUBJECT  =#{subject},</if>
            <if test="content !=null and content != ''">CONTENT  =#{content}</if>
        </set>
        WHERE bno = #{bno}
    </update>

    <delete id="boardDelete" parameterType="int">
        DELETE FROM BOARD WHERE BNO = #{bno}
    </delete>
</mapper>


